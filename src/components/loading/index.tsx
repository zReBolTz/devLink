const Loading = () => {
  return (
    <div className="flex w-full min-h-screen items-center justify-center">
      <h1 className="text-white text-3xl font-bold">Carregando...</h1>
    </div>
  );
};

export default Loading;
